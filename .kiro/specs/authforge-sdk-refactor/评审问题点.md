
---

## 代码评审意见

### 🔴 Critical Bug 1: PKCE 和 OIDC Nonce 硬编码为空

**文件**: `libs/drogon/src/controllers/AuthorizationEndpointController.cc:370-372`

```cpp:370:372:libs/drogon/src/controllers/AuthorizationEndpointController.cc
                        "",  // codeChallenge
                        "",  // codeChallengeMethod
                        "",  // nonce
```

**状态: 仍存在 ✗**  
`AuthorizationEndpointController.authorize()` 中完全没有任何对 `code_challenge`、`code_challenge_method`、`nonce` 参数的提取（搜索确认只有这3行匹配）。PKCE 和 OIDC nonce 仍然不可用。

---

### 🔴 Critical Bug 2: Client Credentials 不验证 scope

**文件**: `libs/drogon/src/controllers/TokenEndpointController.cc:768-769`

```cpp:768:769:libs/drogon/src/controllers/TokenEndpointController.cc
                    std::string requestedScope = req->getParameter("scope");
                    std::string grantedScope = requestedScope.empty() ? "read" : requestedScope;
```

**状态: 仍存在 ✗**  
`client_credentials` grant type 仍不验证 scope 是否在客户端允许列表中，攻击者可直接请求 admin 级别 scope。

---

### 🔴 Critical Bug 3: Device Code 并发竞态

**文件**: `libs/drogon/src/controllers/TokenEndpointController.cc:960-1009`

```cpp:960:977:libs/drogon/src/controllers/TokenEndpointController.cc
              if (status != "approved")
              ...
              // Status is "approved" — issue tokens  ← 非原子操作
              ...
              plugin->saveTokenPair(accessToken, refreshToken, [...deviceCodeHash]() {
                    // 在 saveTokenPair 成功后才删除 device_code ← 迟于 token 发放
                    Mapper<...>(dbClient).deleteBy(Criteria(...deviceCodeHash...), ...);
```

**状态: 仍存在 ✗**  
status 检查（L960）和 device code 删除（L1009）之间没有原子性保护。两个并发请求可能同时通过 `status == "approved"` 检查。

---

### 🔴 Critical Bug 4: `[this]` 违规捕获

**文件**: `libs/drogon/src/controllers/TokenEndpointController.cc:1114`

```cpp:1114:1114:libs/drogon/src/controllers/TokenEndpointController.cc
    plugin->getUserRoles(userId, [this, userId, callback](std::vector<std::string> roles) {
```

**状态: 仍存在 ✗**  
`userInfo()` 中仍然使用 `[this]` 捕获。`TokenEndpointController` 未继承 `enable_shared_from_this`，异步回调完成后可能 use-after-free。

---

### 🟡 High Bug 5: Device Code 缺少客户端认证

**文件**: `libs/drogon/src/controllers/TokenEndpointController.cc:810-814`

```cpp:810:814:libs/drogon/src/controllers/TokenEndpointController.cc
        std::string deviceCode = req->getParameter("device_code");
        if (clientId.empty())
        {
            clientId = req->getParameter("client_id");
        }
```

**状态: 仍存在 ✗**  
device code grant 的 token 交换完全不调用 `validateClient()`，不验证客户端凭证。

---

### 🟡 High Bug 6: `expires_in` 硬编码 3600

**文件**: 三处位置

| 位置 | 行号 | 代码 |
|------|------|------|
| `libs/oauth2/src/protocol/TokenService.cc` | 274 | `json["expires_in"] = (Json::Int64)(3600);` |
| `libs/oauth2/src/protocol/TokenService.cc` | 399 | `json["expires_in"] = (Json::Int64)3600;` |
| `libs/drogon/src/controllers/TokenEndpointController.cc` | 791 | `json["expires_in"] = 3600;` |
| `libs/drogon/src/controllers/TokenEndpointController.cc` | 1026 | `json["expires_in"] = 3600;` |

**状态: 仍存在 ✗**  
四处都硬编码 `3600`，未使用 `accessTokenTtl_` 配置值。

---

### 🟡 High Bug 7: libs/drogon Filter 缺异常处理

**文件**: `libs/drogon/src/filters/OAuth2AuthFilter.cc:18-100`

**状态: 已修复 ✓**  
重构后旧 `OAuth2Plugin/src/filters/OAuth2AuthFilter.cc` 已被删除。现存的唯一 filter 实现已合并了异常处理逻辑：

```cpp:18:100:libs/drogon/src/filters/OAuth2AuthFilter.cc
    try { ... }
    catch (const std::exception &e) { ... }  // L81-90
    catch (...) { ... }                      // L91-100
```

---

### 🟡 Medium Bug 8: `validateAccessToken` 缺生命周期安全

**文件**: `libs/oauth2/src/protocol/TokenService.cc:420-421`

```cpp:419:421:libs/oauth2/src/protocol/TokenService.cc
    auto hashedToken = hashToken(*crypto_, token);
    tokens_->getAccessToken(
      hashedToken, [callback](std::optional<...> t) {
```

**状态: 仍存在 ✗**  
回调中直接捕获 `callback`，未使用 `auto self = shared_from_this()` 保护 `TokenService` 生命周期。与 `exchangeCodeForToken`（L176 `auto self = shared_from_this()`）做法不一致。

---

### 🟡 Medium Bug 9: JWKS 空响应无缓存头

**文件**: `libs/drogon/src/controllers/DiscoveryController.cc:230-236`

```cpp:230:236:libs/drogon/src/controllers/DiscoveryController.cc
    auto plugin = resolvePlugin();
    if (!plugin || !plugin->getJwkManager())
    {
        Json::Value empty;
        empty["keys"] = Json::Value(Json::arrayValue);
        auto resp = ::drogon::HttpResponse::newHttpJsonResponse(empty);
        callback(resp);  // ← 缺少 Cache-Control header
        return;
    }
```

**状态: 仍存在 ✗**

---

### 🟡 Medium Bug 10: `client_credentials` 未设置 `issuedAt`

**文件**: `libs/drogon/src/controllers/TokenEndpointController.cc:778-783`

```cpp:778:783:libs/drogon/src/controllers/TokenEndpointController.cc
                    token.clientId = clientId;
                    token.userId = "client:" + clientId;
                    token.scope = grantedScope;
                    token.expiresAt = now + 3600;
                    // ← token.issuedAt 从未设置！
```

对比 device_code flow（L987 `accessToken.issuedAt = now`）。

**状态: 仍存在 ✗**

---

## 最终汇总

| 严重级别 | Bug | 状态 |
|---------|-----|------|
| 🔴 Critical 1 | PKCE/Nonce 硬编码为空 | **仍存在** |
| 🔴 Critical 2 | client_credentials 不验证 scope | **仍存在** |
| 🔴 Critical 3 | device code 并发竞态 | **仍存在** |
| 🔴 Critical 4 | `[this]` 违规捕获 | **仍存在** |
| 🟡 High 5 | device code 缺客户端认证 | **仍存在** |
| 🟡 High 6 | `expires_in` 硬编码 | **仍存在** |
| 🟡 High 7 | Filter 缺异常处理 | ✅ **已修复** |
| 🟡 Medium 8 | validateAccessToken 缺生命周期安全 | **仍存在** |
| 🟡 Medium 9 | JWKS 空响应无缓存头 | **仍存在** |
| 🟡 Medium 10 | client_credentials 缺 issuedAt | **仍存在** |
