apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fulla.fullname" . }}-config
  labels:
    {{- include "fulla.labels" . | nindent 4 }}
data:
  {{- include "fulla.backendConfigData" . | nindent 2 }}
