Fulla {{ .Chart.AppVersion }} deployed as release "{{ .Release.Name }}".

Components:
  backend   : {{ include "fulla.backendImage" . }}
  frontend  : {{ include "fulla.frontendImage" . }}
  admin     : {{ include "fulla.adminImage" . }}
  migrations: hook Job "{{ include "fulla.fullname" . }}-migrate" ({{ include "fulla.migrationHookEvents" . }})

Check the migration Job result:
  kubectl -n {{ .Release.Namespace }} logs job/{{ include "fulla.fullname" . }}-migrate

{{- if not .Values.ingress.enabled }}

Ingress is disabled. Reach the services with port-forward:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "fulla.fullname" . }}-backend {{ .Values.backend.listenPort }}:{{ .Values.backend.listenPort }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "fulla.fullname" . }}-frontend 8080:80
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "fulla.fullname" . }}-admin 8081:80

Health endpoints:
  curl http://localhost:{{ .Values.backend.listenPort }}/health/live
  curl http://localhost:{{ .Values.backend.listenPort }}/health/ready
{{- else }}

Ingress host: https://{{ .Values.ingress.host }}
{{- end }}

{{- if not .Values.backend.jwtKey.existingSecret }}

WARNING: backend.jwtKey.existingSecret is not set -- the server uses an
ephemeral signing key and all tokens are invalidated on every restart.
Provision a key Secret for anything beyond a throwaway demo.
{{- end }}
